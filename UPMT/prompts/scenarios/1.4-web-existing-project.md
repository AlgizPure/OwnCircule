# UPMT Bootstrap - Сценарий 1.4: Web (GitHub) + Существующий проект

**ПОЛНЫЙ ПРОМПТ (для копирования целиком)**

---

Ты - Claude Code, AI-ассистент для bootstrap проекта через GitHub API.

## КОНТЕКСТ

**Сценарий:** Web (GitHub) + Существующий проект  
**Код:** 1.4  
**Режим:** WEB_GITHUB (через браузер)  
**Existing project:** true  

Я загрузил UPMT шаблон в GitHub репозиторий. Raw data и **существующий код** находятся в репозитории.

---

## ТВОЯ ЗАДАЧА

Выполни полный bootstrap процесс с анализом существующего кода через GitHub API.

---

## ИНСТРУКЦИИ ПО ВЫПОЛНЕНИЮ

### ШАГ 1: Прочитай архитектурные файлы через GitHub API

**1.1: Прочитай оркестратор:**
```bash
gh api /repos/{owner}/{repo}/contents/UPMT/prompts/orchestrator.md --jq '.content' | base64 -d
```

**1.2: Прочитай Web адаптер:**
```bash
gh api /repos/{owner}/{repo}/contents/UPMT/prompts/adapters/web-adapter.md --jq '.content' | base64 -d
```

---

### ШАГ 2: Установи параметры сценария

```yaml
scenario:
  name: "Web + Existing Project"
  code: "1.4"
  mode: "WEB_GITHUB"
  existing_project: true
  
data_sources:
  - raw_data: "UPMT/bootstrap/00_RAW_DATA_TEMPLATE/ (через GitHub API)"
  - code_analysis: true
  - code_location: "src/, app/, components/ (через GitHub API)"
  
github:
  repo: "{owner}/{repo}"
  api_mode: true
```

---

### ШАГ 3: НАЧИНАЙ ВЫПОЛНЕНИЕ

Следуй оркестратору, выполняя фазы последовательно:

**⚠️ ВСЕ ОПЕРАЦИИ ЧЕРЕЗ GITHUB API!**

**PHASE 1: ANALYSIS** (1-2 часа) ⚠️ С CODE ANALYSIS
```bash
gh api /repos/{owner}/{repo}/contents/UPMT/prompts/phases/phase-1-analysis.md --jq '.content' | base64 -d
```
- Читай raw data через GitHub API
- **Анализируй существующий код через GitHub API** (src/, app/)
- Читай package.json через GitHub API
- Извлеки ВСЕ функции (raw data + code)
- Сопоставь requirements vs реальность
- Создай extracted_features.md через PUT (с статусами)
- Покажи пользователю для APPROVED
- Создай modules_list.md через PUT (с статусами: Implemented/Partial/Not Started)

**PHASE 2: INTERVIEW** (30-60 минут)
```bash
gh api /repos/{owner}/{repo}/contents/UPMT/prompts/phases/phase-2-interview.md --jq '.content' | base64 -d
```
- Задай вопросы (включая вопросы о найденных в коде фичах)
- Обнови metadata.yaml через PUT (с SHA)

**PHASE 3: TECH VERIFICATION** (45-60 минут)
```bash
gh api /repos/{owner}/{repo}/contents/UPMT/prompts/phases/phase-3-tech-verification.md --jq '.content' | base64 -d
```
- Используй tech stack из package.json (через GitHub API)
- Verification для актуализации ({CURRENT_MONTH_YEAR})

**PHASE 4: SYNTHESIS** (15 минут)
```bash
gh api /repos/{owner}/{repo}/contents/UPMT/prompts/phases/phase-4-synthesis.md --jq '.content' | base64 -d
```
- Объедини raw data + code analysis

**PHASE 5: DOCUMENTATION** (2-4 часа)
```bash
gh api /repos/{owner}/{repo}/contents/UPMT/prompts/phases/phase-5-documentation.md --jq '.content' | base64 -d
```
- Документация с учётом существующего кода
- Статусы: Implemented/Partial/Not Started
- Создавай через PUT requests

**PHASE 5.5: DESIGN** (conditional) ⚠️ ВЫПОЛНЯЕТСЯ
```bash
gh api /repos/{owner}/{repo}/contents/UPMT/prompts/phases/phase-5.5-design.md --jq '.content' | base64 -d
```
- Анализируй design из кода через GitHub API (styles, components)
- Документируй ЧТО УЖЕ ЕСТЬ

**PHASE 5.7: BACKEND** (conditional)
```bash
gh api /repos/{owner}/{repo}/contents/UPMT/prompts/phases/phase-5.7-backend.md --jq '.content' | base64 -d
```
- Выполни если backend detected

**PHASE 6: SETUP** (15 минут)
```bash
gh api /repos/{owner}/{repo}/contents/UPMT/prompts/phases/phase-6-setup.md --jq '.content' | base64 -d
```

**PHASE 7: VALIDATION** (30 минут)
```bash
gh api /repos/{owner}/{repo}/contents/UPMT/prompts/phases/phase-7-validation.md --jq '.content' | base64 -d
```

**PHASE 8: REPORT** (10 минут)
```bash
gh api /repos/{owner}/{repo}/contents/UPMT/prompts/phases/phase-8-report.md --jq '.content' | base64 -d
```
- Включи code analysis statistics

---

### ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА

1. **Используй Web адаптер** для всех операций
2. **ВСЕ операции через GitHub API** (GET/PUT)
3. **Анализируй существующий код через GitHub API**
4. **Сравнивай requirements vs реальность**
5. **Content должен быть base64 encoded**
6. **Показывай прогресс каждые 30 минут**

---

## 🚀 НАЧАЛО РАБОТЫ

```bash
→ НАЧИНАЙ С PHASE 1: ANALYSIS (с code analysis через GitHub API)
→ Читай через GitHub API: UPMT/prompts/phases/phase-1-analysis.md
→ ВЫПОЛНЯЙ!
```

**НАЧИНАЙ! 🚀**

