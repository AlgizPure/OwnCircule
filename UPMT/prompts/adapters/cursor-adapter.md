# 🎨 CURSOR ADAPTER - Работа в Cursor 2.0 IDE

**Назначение:** Специфичные инструкции для работы в Cursor 2.0 с Claude Code extension или Claude Code Chat extension

**Версия:** 1.0  
**Дата:** 2025-11-14  
**Совместимость:** Cursor 2.0+, VS Code с Claude Code extensions

---

## ⚠️ ЯЗЫК ОБЩЕНИЯ (КРИТИЧНО!)

**ВСЕ сообщения пользователю должны быть на РУССКОМ ЯЗЫКЕ!**

См. также: `UPMT/prompts/orchestrator.md` - ШАГ 0.0: ЯЗЫК ОБЩЕНИЯ

---

## 🎯 ОСОБЕННОСТИ CURSOR 2.0

**Cursor 2.0 - это гибрид CLI и IDE:**
- ✅ **Локальный доступ к файлам** (как CLI) - используй `read_file()`, `write()`
- ✅ **Встроенный AI чат** (Claude Code Chat extension)
- ✅ **Git операции** через встроенный терминал или VS Code Git UI
- ✅ **Может работать с GitHub репозиториями** локально (не нужен API)
- ✅ **VS Code API** доступен через расширения (опционально)
- ✅ **Быстрее чем Web** - нет API latency

**Отличия от CLI:**
- ✅ Может использовать VS Code API через расширения
- ✅ Встроенный AI чат (не нужен отдельный терминал)
- ✅ Может работать с GitHub репозиториями локально

**Отличия от Web:**
- ✅ НЕ нужен GitHub API для чтения файлов
- ✅ Может выполнять локальные команды (npm, git)
- ✅ Быстрее чем Web (нет API latency)

---

## 📁 ФАЙЛОВЫЕ ОПЕРАЦИИ

### Чтение файлов

**Используй стандартные инструменты (как в CLI):**

```python
# Прочитай файл
read_file("path/to/file.md")

# Прочитай папку
list_dir("path/to/directory")

# Поиск файлов
glob_file_search("**/*.md", target_directory="path")
```

**Примеры:**

```python
# Читай raw data
read_file("UPMT/bootstrap/00_RAW_DATA_TEMPLATE/metadata.yaml")
read_file("UPMT/bootstrap/00_RAW_DATA_TEMPLATE/chats/chat1.txt")

# Читай существующий код (если existing project)
list_dir("src")
read_file("src/index.ts")
read_file("package.json")
```

**⚠️ ОБРАБОТКА БОЛЬШИХ ФАЙЛОВ:**

**Алгоритм автоматического чтения (ИСПОЛЬЗУЙ ВСЕГДА):**

```python
def safe_read_file(file_path):
    """
    Читает файл целиком или по частям, если большой.
    АВТОМАТИЧЕСКИ обрабатывает ошибки размера.
    """
    try:
        # Попытка прочитать целиком
        return read_file(file_path)
    except (FileTooLargeError, TokenLimitExceededError) as e:
        # Файл большой - читай по частям
        print(f"⚠️ Файл {file_path} слишком большой, читаю по частям...")
        
        # 1. Определи размер файла (строки)
        # В Cursor можно использовать list_dir для получения размера
        line_count = get_line_count(file_path)  # Используй read_file с offset/limit
        
        # 2. Читай порциями по 2000 строк (безопасный размер)
        chunks = []
        chunk_size = 2000
        
        for start_line in range(1, line_count + 1, chunk_size):
            end_line = min(start_line + chunk_size - 1, line_count)
            limit = end_line - start_line + 1
            
            chunk = read_file(
                file_path=file_path,
                offset=start_line,
                limit=limit
            )
            chunks.append(chunk)
            
            # Логируй прогресс
            print(f"📖 Прочитано {end_line}/{line_count} строк из {file_path}")
        
        # 3. Объедини все порции
        full_content = "\n".join(chunks)
        print(f"✅ Файл {file_path} прочитан полностью ({line_count} строк)")
        return full_content

# Использование:
content = safe_read_file("UPMT/bootstrap/00_RAW_DATA_TEMPLATE/chats/large_chat.txt")
```

**Если получил ошибку:**
- `File content (XXX KB) exceeds maximum allowed size (256KB)`
- `File content (XXXXX tokens) exceeds maximum allowed tokens (25000)`

**ТОГДА:**

1. **Определи размер файла:**
```python
# В Cursor можно использовать read_file с offset/limit для определения размера
# Или используй list_dir для получения метаданных файла
```

2. **Читай по частям (по 2000 строк):**
```python
# Порция 1: строки 1-2000
chunk1 = read_file("path/to/file.txt", offset=1, limit=2000)

# Порция 2: строки 2001-4000
chunk2 = read_file("path/to/file.txt", offset=2001, limit=2000)

# ... и так далее
```

---

### Создание и обновление файлов

**Используй стандартные инструменты:**

```python
# Создай файл
write("path/to/file.md", content)

# Обнови файл
search_replace("path/to/file.md", old_string, new_string)

# Обнови несколько вхождений
search_replace("path/to/file.md", old_string, new_string, replace_all=True)
```

**Примеры:**

```python
# Создай extracted_features.md
write("UPMT/bootstrap/00_RAW_DATA_TEMPLATE/extracted_features.md", features_content)

# Обнови metadata.yaml
search_replace("UPMT/bootstrap/00_RAW_DATA_TEMPLATE/metadata.yaml", 
                "project_name: Template", 
                "project_name: My Project")
```

**⚠️ ВАЖНО:**
- ✅ Используй `write()` для создания файлов
- ✅ Используй `search_replace()` для обновления
- ❌ НЕ используй терминал для создания файлов (`echo`, `cat`, `>`)

---

## 🔧 GIT ОПЕРАЦИИ

### Через встроенный терминал Cursor

**Cursor имеет встроенный терминал (как VS Code):**

```bash
# Добавь файлы
git add docs/core/

# Коммит
git commit -m "docs(bootstrap): PHASE 1 complete - feature extraction"

# Push
git push origin main
```

**Или через VS Code Git UI:**
- Используй Source Control панель в Cursor
- Stage changes
- Commit с сообщением
- Push через UI

**⚠️ RETRY LOGIC если push failed:**

```python
def safe_push(max_retries=3):
    for attempt in range(max_retries):
        try:
            # Выполни git push через терминал
            run_terminal_cmd("git push origin main")
            return True
        except NetworkError:
            if attempt < max_retries - 1:
                wait(30)  # 30 секунд
                retry
            else:
                alert_user("Push failed after 3 attempts")
                save_state(".bootstrap-state.json")
                return False
```

---

## 🎨 CURSOR-SPECIFIC FEATURES

### Использование VS Code API (если доступно)

**Cursor может использовать VS Code API через расширения:**

```python
# Опционально: если расширение предоставляет VS Code API
# Можно использовать для более продвинутых операций

# Пример (если доступно):
# vscode.workspace.openTextDocument()
# vscode.window.showInformationMessage()
```

**⚠️ ВАЖНО:**
- VS Code API доступен только если расширение его предоставляет
- По умолчанию используй стандартные инструменты (`read_file`, `write`)
- VS Code API - опциональная оптимизация, не обязательна

---

### Работа с GitHub репозиториями локально

**Cursor может работать с GitHub репозиториями локально:**

```bash
# Клонируй репозиторий (если нужно)
git clone https://github.com/owner/repo.git

# Или открой существующий клонированный репозиторий
# Cursor автоматически определит git репозиторий

# Все операции через локальный git, НЕ через GitHub API
git add .
git commit -m "message"
git push
```

**Отличие от Web adapter:**
- ❌ НЕ используй GitHub API (`gh api`)
- ✅ Используй локальный git
- ✅ Быстрее и проще

---

## 📊 ПРОГРЕСС TRACKING

**Показывай прогресс каждые 30 минут:**

```markdown
⏱️ BOOTSTRAP PROGRESS UPDATE (Cursor 2.0)

**Текущая фаза:** PHASE X - [название]
**Прогресс:** [X%]
**Время работы:** [HH:MM]

**Последние действия:**
- ✅ Создано: docs/core/00_PROJECT_ESSENCE.md
- ✅ Создано: docs/core/01_PRD.md
- 🔄 Создаю: docs/requirements/module_1_requirements.md

**Следующие шаги:**
- [ ] Создать requirements для модуля 2
- [ ] ...

**Checkpoint commits:** 5
```

---

## 🚨 КРИТИЧЕСКИЕ ПРАВИЛА CURSOR

1. **ИСПОЛЬЗУЙ СТАНДАРТНЫЕ ИНСТРУМЕНТЫ**
   - ✅ `read_file()` - для чтения
   - ✅ `write()` - для создания
   - ✅ `search_replace()` - для обновления
   - ✅ `list_dir()` - для просмотра структуры
   - ✅ `glob_file_search()` - для поиска

2. **НЕ ИСПОЛЬЗУЙ TERMINAL ДЛЯ ФАЙЛОВЫХ ОПЕРАЦИЙ**
   - ❌ `cat file.md`
   - ❌ `echo "content" > file.md`
   - ✅ `read_file("file.md")`
   - ✅ `write("file.md", "content")`

3. **GIT OPERATIONS - ЧЕРЕЗ TERMINAL ИЛИ UI**
   - ✅ `git add`, `git commit`, `git push` (терминал)
   - ✅ VS Code Git UI (Source Control панель)
   - ❌ GitHub API (не нужен в Cursor)

4. **ПИШИ ПОЛНЫЕ ФАЙЛЫ**
   - Не используй `[...]` или placeholders
   - Не используй `// ... rest of content`
   - Пиши всё полностью

5. **ИСПОЛЬЗУЙ ЛОКАЛЬНЫЙ GIT**
   - ❌ НЕ используй GitHub API
   - ✅ Используй локальный git
   - ✅ Быстрее и проще

---

## 🔄 CHECKPOINT СИСТЕМА

**⚠️ КРИТИЧНО: Checkpoint ДОЛЖЕН быть сохранен после КАЖДОЙ фазы и КАЖДОГО batch!**

**Используй `save_checkpoint_cli()` (как в CLI mode) - Cursor работает локально.**

**Правила коммитов:**

**PHASE 1-4, 6-8:**
- 1 фаза = 1 коммит

**PHASE 5 (много файлов):**
- Батчи по 6 модулей
- После каждого батча - коммит

**PHASE 5.5, 5.7:**
- Группируй по секциям
- 1 секция = несколько файлов = 1 коммит

**Формат commit message:**

```
docs(bootstrap): PHASE X [название] - [краткое описание]

- Создано: [N файлов]
- Обновлено: [M файлов]
- Следующая фаза: PHASE X+1
```

---

## 📚 ССЫЛКИ

- **Оркестратор:** `UPMT/prompts/orchestrator.md` (прочитай через `read_file()`)
- **Фазы:** `UPMT/prompts/phases/phase-X-*.md` (прочитай через `read_file()`)
- **Этот адаптер используется для сценариев:** 1.5, 1.6

---

## ⚠️ ОСОБЕННОСТИ CURSOR РЕЖИМА

1. **Можно запустить npm install**
   - ✅ Локальные команды работают
   - ✅ Можно установить зависимости
   - ✅ Можно запустить dev server

2. **Можно выполнить команды пользователя**
   - ✅ Терминал доступен
   - ✅ Можно запускать скрипты
   - ✅ Можно использовать npm, git, и т.д.

3. **Все файлы локально**
   - ✅ Быстрый доступ
   - ✅ Нет API latency
   - ✅ Можно использовать VS Code features

4. **Быстрее чем Web**
   - ✅ Нет API requests
   - ✅ Прямой доступ к файлам
   - ✅ Мгновенные операции

---

**Адаптер прочитан. Возвращайся к оркестратору и начинай PHASE 1 (локально в Cursor).**

