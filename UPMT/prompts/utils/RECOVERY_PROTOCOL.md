# 🛡️ RECOVERY PROTOCOL - Восстановление после зависания Claude Code

**Версия:** 2.0  
**Дата:** 2025-11-15  
**Для:** Claude Code Web + CLI + Cursor

---

## 🚨 ПРОБЛЕМА

**Claude Code Web зависает:**
- Появляется кнопка "Retry Connection"
- Команды не выполняются
- Сессия не реагирует на ввод
- Прогресс теряется при закрытии

**Причины:**
- Сетевые проблемы
- API rate limits
- Timeout на длительных операциях
- Перегрузка контекста

---

## 💾 CHECKPOINT СИСТЕМА (автоматическая)

**Автоматические сохранения:**

### 1. Git Checkpoints (существующие)

После каждой фазы Claude Code делает коммит:

```bash
git commit -m "docs(bootstrap): PHASE X [название] complete"
```

**Checkpoint точки:**
- ✅ После PHASE 1 (Analysis + Approval)
- ✅ После PHASE 2 (Interview)
- ✅ После PHASE 3 (Tech Verification)
- ✅ После PHASE 4 (Synthesis)
- ✅ После PHASE 5 (каждые 6 модулей + финальный)
- ✅ После PHASE 5.5 (Design, если выполнялась)
- ✅ После PHASE 5.7 (Backend, если выполнялась)
- ✅ После PHASE 6 (Setup Instructions)
- ✅ После PHASE 7 (Validation)
- ✅ После PHASE 8 (Final Report)

### 2. State JSON Checkpoints (НОВОЕ!)

**Создается автоматически:**

```
.upmt/
├── checkpoints/
│   ├── latest.json                    # Последний checkpoint
│   ├── phase-1-complete.json
│   ├── phase-2-complete.json
│   ├── phase-5-batch-1.json
│   ├── phase-5-batch-2.json
│   └── ...
└── recovery/
    └── last-session.log               # Логи последней сессии
```

**Формат `latest.json`:**

```json
{
  "phase": 5,
  "phase_name": "PHASE 5: Documentation",
  "batch": 2,
  "timestamp": "2025-11-15T14:30:00Z",
  "session_id": "web-20251115-143000",
  "state": {
    "current_module": "Authentication",
    "modules_completed": ["Dashboard", "User Profile"],
    "total_modules": 8,
    "files_created": [
      "docs/requirements/dashboard.md",
      "docs/requirements/user-profile.md"
    ]
  },
  "stats": {
    "total_files": 47,
    "total_lines": 8540,
    "elapsed_time": "02:15:30"
  },
  "next_action": "Continue with module: Settings (3 of 8)"
}
```

---

## 🔍 ОПРЕДЕЛЕНИЕ БЕЗОПАСНОЙ ТОЧКИ ВОССТАНОВЛЕНИЯ

**Алгоритм определения:**

### ШАГ 1: Проверь Git History

```bash
# Посмотри последние коммиты
git log --oneline -10 --grep="bootstrap"
```

**Ищи последний checkpoint коммит:**
```
a1b2c3d docs(bootstrap): PHASE 5 batch 2 complete - 2/3 batches done
e4f5g6h docs(bootstrap): PHASE 5 batch 1 complete - 1/3 batches done
i7j8k9l docs(bootstrap): PHASE 4 complete - Synthesis finished
```

**Последний безопасный checkpoint:** `PHASE 5 batch 2`

### ШАГ 2: Проверь State JSON

```bash
# Прочитай latest.json (если есть)
cat .upmt/checkpoints/latest.json
```

**Если файл существует:**
- Используй информацию из JSON
- Это самая актуальная точка восстановления

**Если файла нет:**
- Используй последний Git checkpoint
- Восстановление с начала последней завершенной фазы

### ШАГ 3: Проверь созданные файлы

```bash
# Проверь что создано
ls -la docs/requirements/
ls -la UPMT/synthesis/
ls -la UPMT/bootstrap/00_RAW_DATA_TEMPLATE/
```

**Сравни со списком в `latest.json`:**
- Если файлы совпадают → checkpoint валиден
- Если файлов больше → были промежуточные сохранения
- Если файлов меньше → checkpoint некорректен (откат на предыдущий)

---

## 🚀 RECOVERY PROTOCOL (пошагово)

### СЦЕНАРИЙ A: Claude Code Web зависла

**Симптомы:**
- ❌ Кнопка "Retry Connection"
- ❌ Команды не выполняются
- ❌ Нет ответа >5 минут

**Действия:**

#### 1. НЕ ЗАКРЫВАЙ ВКЛАДКУ сразу!

Попробуй:
1. Нажми "Retry Connection" (1-2 попытки)
2. Подожди 2-3 минуты
3. Если не помогло → переходи к шагу 2

#### 2. Открой НОВУЮ вкладку/окно

**НЕ закрывай старую вкладку!**

В новом окне:
1. Открой тот же репозиторий в Claude Code Web
2. Запусти Recovery Protocol (см. ниже)

#### 3. Запусти Recovery Protocol

**ПРОМПТ ДЛЯ НОВОЙ СЕССИИ:**

```markdown
🛡️ RECOVERY MODE - Восстановление bootstrap процесса

Предыдущая сессия зависла. Необходимо восстановить bootstrap с безопасной точки.

**ШАГ 1: Определение checkpoint**

1. Прочитай `.upmt/checkpoints/latest.json` (если есть)
2. Посмотри последние Git коммиты: `git log --oneline -10 --grep="bootstrap"`
3. Определи последний успешный checkpoint

**ШАГ 2: Анализ состояния**

1. Проверь какие файлы созданы в `docs/`, `UPMT/synthesis/`, `.upmt/`
2. Сравни со списком файлов из checkpoint
3. Определи: с какой фазы/батча нужно продолжить

**ШАГ 3: Восстановление контекста**

Прочитай ВСЕ файлы контекста для текущей фазы:
- `UPMT/prompts/orchestrator.md`
- `UPMT/prompts/adapters/[cli|web]-adapter.md`
- `UPMT/prompts/phases/phase-[N]-*.md` (для текущей фазы)
- `UPMT/bootstrap/00_RAW_DATA_TEMPLATE/extracted_features.md`
- `UPMT/bootstrap/00_RAW_DATA_TEMPLATE/modules_list.md`
- `UPMT/synthesis/PROJECT_SYNTHESIS.md` (если создан)

**ШАГ 4: Отчет о восстановлении**

Выведи:
```
╔══════════════════════════════════════════════════════════════╗
║              BOOTSTRAP RECOVERY - STATUS REPORT               ║
╚══════════════════════════════════════════════════════════════╝

📍 CHECKPOINT DETECTED:
   Phase: [название фазы]
   Batch: [N из M] (если применимо)
   Timestamp: [дата/время]
   Files created: [N файлов]

📊 PROGRESS SUMMARY:
   ✅ Completed:
      - PHASE 1: Analysis
      - PHASE 2: Interview
      - PHASE 3: Tech Verification
      - PHASE 4: Synthesis
      - PHASE 5: Documentation (2/3 batches)
   
   🔄 TO RESUME:
      - PHASE 5: Documentation (batch 3/3)
      - PHASE 5.5: Design System (conditional)
      - PHASE 5.7: Backend Documentation (conditional)
      - PHASE 6: Setup Instructions
      - PHASE 7: Validation
      - PHASE 8: Final Report

🎯 NEXT ACTION:
   Resume from: PHASE 5, batch 3
   Module: [название модуля]
   Expected files to create: [список]

🔍 VALIDATION:
   - [ ] Git checkpoint verified
   - [ ] JSON state validated
   - [ ] Files integrity checked
   - [ ] Context files loaded
   - [ ] Ready to resume

Would you like me to continue from this checkpoint? (yes/no)
```

**ШАГ 5: Продолжение bootstrap**

После подтверждения:
1. Продолжи с определенной точки
2. НЕ ПЕРЕСОЗДАВАЙ уже созданные файлы
3. Сохраняй checkpoint после каждого батча
4. Следуй orchestrator.md для текущей фазы
```

---

### СЦЕНАРИЙ B: Полная потеря контекста (нет JSON checkpoint)

**Ситуация:**
- Нет `.upmt/checkpoints/latest.json`
- Есть только Git коммиты

**Промпт для восстановления:**

```markdown
🛡️ RECOVERY MODE - Восстановление с Git checkpoint

Предыдущая сессия прервана, JSON checkpoint отсутствует. Восстановление по Git истории.

**ШАГ 1: Определение последнего checkpoint**

```bash
git log --oneline --grep="bootstrap" -20
```

Найди последний коммит с паттерном:
- `docs(bootstrap): PHASE X complete`
- `docs(bootstrap): PHASE X batch Y complete`

**ШАГ 2: Анализ файловой системы**

Проверь наличие ключевых файлов:
```bash
ls -la docs/requirements/
ls -la docs/modules/
ls -la UPMT/synthesis/
ls -la UPMT/bootstrap/00_RAW_DATA_TEMPLATE/
```

**ШАГ 3: Определение фазы**

По наличию файлов определи:
- PHASE 1: `extracted_features.md`, `modules_list.md` существуют?
- PHASE 2: `metadata.yaml` заполнен (не template)?
- PHASE 3: `final-tech-stack.md` существует?
- PHASE 4: `PROJECT_SYNTHESIS.md` существует?
- PHASE 5: `docs/requirements/` заполнена? Сколько модулей задокументировано?
- PHASE 6: `UPMT_FINAL_STEPS.md` существует?
- PHASE 7: `BOOTSTRAP_REPORT.md` существует?

**ШАГ 4: Создание recovery checkpoint**

Создай новый checkpoint:
```json
{
  "phase": [определенная фаза],
  "phase_name": "[название]",
  "recovery_mode": true,
  "recovered_from": "git_history",
  "timestamp": "[текущее время]",
  "state": {
    "detected_files": [список найденных файлов],
    "resume_point": "[с какой фазы продолжить]"
  }
}
```

Сохрани в `.upmt/checkpoints/recovery.json`

**ШАГ 5: Продолжение**

См. СЦЕНАРИЙ A, ШАГ 4-5
```

---

### СЦЕНАРИЙ C: Critical Error (файлы повреждены)

**Ситуация:**
- Файлы созданы, но неполные или повреждены
- JSON checkpoint указывает на несуществующие файлы
- Git история не совпадает с состоянием файлов

**Промпт:**

```markdown
🛡️ CRITICAL RECOVERY - Откат к стабильной точке

Обнаружены поврежденные файлы. Необходим откат к стабильному checkpoint.

**ШАГ 1: Определение стабильного checkpoint**

```bash
git log --oneline --grep="bootstrap" -20
```

Найди последний "phase complete" коммит (НЕ batch):
- ✅ `docs(bootstrap): PHASE 4 complete`
- ❌ `docs(bootstrap): PHASE 5 batch 2 complete` (skip, может быть неполным)

**ШАГ 2: Откат к стабильной точке**

```bash
# Создай backup текущих изменений
git stash push -m "recovery-backup-$(date +%Y%m%d-%H%M%S)"

# Откатись к стабильному checkpoint
git reset --hard [commit_hash]
```

**ШАГ 3: Верификация состояния**

Проверь:
- [ ] `extracted_features.md` валиден (не пустой, не template)
- [ ] `modules_list.md` валиден
- [ ] `metadata.yaml` заполнен
- [ ] `PROJECT_SYNTHESIS.md` существует (если откат после PHASE 4)

**ШАГ 4: Перезапуск с чистой точки**

Запусти заново с текущей фазы (см. СЦЕНАРИЙ A, ШАГ 3-5)

**ШАГ 5: Сохранение backup для анализа**

```bash
# Восстанови backup для анализа
git stash pop

# Сохрани в отдельную папку
mkdir -p .upmt/recovery/corrupted-$(date +%Y%m%d)
cp -r docs/ .upmt/recovery/corrupted-$(date +%Y%m%d)/
```
```

---

## 📋 CHECKLIST ПЕРЕД ВОССТАНОВЛЕНИЕМ

**Перед запуском recovery:**

- [ ] **1. Определил последний checkpoint**
  - [ ] Проверил `.upmt/checkpoints/latest.json`
  - [ ] Проверил `git log --grep="bootstrap"`
  - [ ] Определил phase и batch для восстановления

- [ ] **2. Проверил файловую систему**
  - [ ] Прошелся по `docs/`, `UPMT/synthesis/`, `.upmt/`
  - [ ] Сравнил с checkpoint списком файлов
  - [ ] Подтвердил: файлы не повреждены

- [ ] **3. Загрузил контекст**
  - [ ] Прочитал `orchestrator.md`
  - [ ] Прочитал adapter (`cli-adapter.md` / `web-adapter.md`)
  - [ ] Прочитал phase инструкции для текущей фазы
  - [ ] Прочитал контекстные файлы (extracted_features, modules_list, synthesis)

- [ ] **4. Готов к продолжению**
  - [ ] Понимаю с какой фазы продолжить
  - [ ] Понимаю какие файлы создавать дальше
  - [ ] Знаю следующий action

---

## 📝 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Восстановление с середины PHASE 5

**Сценарий:**
- Bootstrap дошел до PHASE 5, batch 2 из 4
- Claude Code Web зависла
- Есть checkpoint JSON

**Действия:**

1. Открываешь новую вкладку Claude Code Web
2. Вставляешь:

```markdown
🛡️ RECOVERY MODE

Предыдущая сессия зависла в PHASE 5. Восстанавливаю с checkpoint.

Прочитай и выполни: UPMT/prompts/utils/RECOVERY_PROTOCOL.md (СЦЕНАРИЙ A)
```

3. Claude анализирует checkpoint, выводит статус
4. Подтверждаешь: "yes, continue"
5. Bootstrap продолжается с batch 3/4

---

### Пример 2: Полная потеря контекста (нет JSON)

**Сценарий:**
- Bootstrap прервался
- Файл `.upmt/checkpoints/latest.json` удален или не создавался
- Есть только Git коммиты

**Действия:**

1. Открываешь Claude Code (новая сессия)
2. Вставляешь:

```markdown
🛡️ RECOVERY MODE - Git History Recovery

Bootstrap прервался, JSON checkpoint отсутствует. Восстанавливаю по Git истории.

Прочитай и выполни: UPMT/prompts/utils/RECOVERY_PROTOCOL.md (СЦЕНАРИЙ B)
```

3. Claude определяет последний Git checkpoint
4. Анализирует созданные файлы
5. Создает recovery checkpoint
6. Продолжает bootstrap

---

### Пример 3: Критическая ошибка (откат)

**Сценарий:**
- PHASE 5 создала поврежденные файлы
- Нужно откатиться к PHASE 4 и перезапустить

**Действия:**

1. В терминале:

```bash
git log --oneline --grep="bootstrap" -10
# Находишь: a1b2c3d docs(bootstrap): PHASE 4 complete
```

2. В Claude Code:

```markdown
🛡️ CRITICAL RECOVERY - Откат к PHASE 4

Обнаружены поврежденные файлы в PHASE 5. Откатываюсь к последнему стабильному checkpoint.

Commit hash для отката: a1b2c3d

Прочитай и выполни: UPMT/prompts/utils/RECOVERY_PROTOCOL.md (СЦЕНАРИЙ C)
```

3. Claude откатывает Git
4. Создает backup поврежденных файлов
5. Перезапускает PHASE 5 с чистого состояния

---

## 🎯 ТЕСТИРОВАНИЕ RECOVERY

**Для проверки recovery protocol:**

### Тест 1: Симуляция зависания

1. Запусти bootstrap
2. Дождись PHASE 5, batch 1
3. Закрой Claude Code (симуляция зависания)
4. Открой новую сессию
5. Запусти recovery
6. Проверь: продолжил ли с batch 2?

### Тест 2: Симуляция потери JSON

1. Запусти bootstrap до PHASE 3
2. Удали `.upmt/checkpoints/latest.json`
3. Закрой Claude Code
4. Открой новую сессию
5. Запусти recovery (СЦЕНАРИЙ B)
6. Проверь: восстановил ли по Git?

### Тест 3: Откат к предыдущей фазе

1. Запусти bootstrap до PHASE 5
2. Симулируй поврежденные файлы (удали `docs/requirements/module-1.md`)
3. Запусти recovery (СЦЕНАРИЙ C)
4. Проверь: откатился ли к PHASE 4?

---

## ✅ УСПЕШНОЕ ВОССТАНОВЛЕНИЕ

**Признаки успешного recovery:**

1. ✅ Claude определил правильный checkpoint
2. ✅ Прочитал все контекстные файлы
3. ✅ НЕ пересоздал уже существующие файлы
4. ✅ Продолжил с правильной фазы/батча
5. ✅ Создал новый checkpoint после первого батча
6. ✅ Bootstrap завершился до конца

**Провал recovery (признаки):**

1. ❌ Claude начал с PHASE 1 (игнорировал checkpoint)
2. ❌ Пересоздал уже существующие файлы
3. ❌ Пропустил модули из `modules_list.md`
4. ❌ Не создал checkpoint после восстановления
5. ❌ Не прочитал контекстные файлы

**При провале:**
- Попробуй СЦЕНАРИЙ C (откат)
- Или запусти bootstrap заново с PHASE 1

---

## 📌 ИТОГОВЫЙ PROMPT ДЛЯ ПОЛЬЗОВАТЕЛЯ

**Копируй и вставляй в новую сессию при зависании:**

```markdown
🛡️ UPMT RECOVERY MODE - Восстановление bootstrap процесса

Предыдущая сессия прервана. Необходимо восстановить с безопасной точки.

**Инструкции:**
1. Прочитай: UPMT/prompts/utils/RECOVERY_PROTOCOL.md
2. Выполни: СЦЕНАРИЙ A (если есть `.upmt/checkpoints/latest.json`)
   ИЛИ: СЦЕНАРИЙ B (если только Git checkpoints)
   ИЛИ: СЦЕНАРИЙ C (если критическая ошибка)
3. Выведи recovery status report
4. Дождись моего подтверждения
5. Продолжи bootstrap с определенной точки

**Критично:**
- НЕ начинай с PHASE 1 если есть checkpoint!
- НЕ пересоздавай уже существующие файлы!
- ИСПОЛЬЗУЙ контекст из предыдущих фаз!
- СОЗДАВАЙ checkpoint после каждого батча!

Начинай recovery analysis.
```

---

**Версия:** 2.0  
**Тестирование:** Required  
**Приоритет:** Critical  
**Эффект:** Предотвращает потерю прогресса при зависании Claude Code Web

---

**Готово к интеграции в UPMT v3.0.2+** ✅

