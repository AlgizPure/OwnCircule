# 🧪 TESTING RECOVERY PROTOCOL

**Версия:** 1.0  
**Дата:** 2025-11-15  
**Назначение:** Тестовые сценарии для проверки Recovery Protocol

---

## 📋 TEST SUITE OVERVIEW

Этот файл содержит 4 основных теста для проверки Recovery Protocol:

1. **Test 1:** Симуляция зависания в PHASE 5
2. **Test 2:** Восстановление по Git (без JSON checkpoint)
3. **Test 3:** Откат к стабильной точке (критическая ошибка)
4. **Test 4:** Полный цикл с checkpoints

---

## 🧪 TEST 1: Симуляция зависания в PHASE 5

### Цель

Проверить автоматическое восстановление bootstrap процесса после зависания Claude Code Web в середине PHASE 5.

### Предварительные условия

- Проект: Brain-Rent (или любой с raw data)
- UPMT с интегрированным Recovery Protocol
- Bootstrap должен дойти до PHASE 5, batch 1

### Шаги выполнения

#### Шаг 1: Подготовка

```bash
cd "C:\Users\333\Documents\My projects\Brain-Rent"

# Убедиться что UPMT файлы обновлены
git pull origin main

# Создать тестовую ветку
git checkout -b test-recovery-protocol-phase5
```

#### Шаг 2: Запуск bootstrap

В Claude Code Web вставить:

```markdown
Прочитай и выполни: UPMT/start/1.3.md

Проект: Brain-Rent
Сценарий: Web + New Project (1.3)
```

#### Шаг 3: Дождаться PHASE 5, batch 1 complete

**Ожидаемые признаки:**
- Создан checkpoint: `.upmt/checkpoints/phase-5-batch-1.json`
- Git коммит: `docs(bootstrap): PHASE 5 batch 1 complete`
- Созданы файлы requirements для первых модулей

**Проверка в терминале:**

```bash
# Проверить checkpoint
cat .upmt/checkpoints/latest.json

# Проверить Git
git log --oneline -5 --grep="bootstrap"

# Проверить созданные файлы
ls docs/requirements/
```

#### Шаг 4: Симуляция зависания

**Действия:**
1. Закрой вкладку Claude Code Web (ИЛИ дождись естественного зависания)
2. НЕ нажимай "Retry Connection"
3. Подожди 1 минуту

#### Шаг 5: Восстановление

**Откройте НОВУЮ вкладку/окно Claude Code Web:**

Вставить:

```markdown
🛡️ RECOVERY MODE - Восстановление bootstrap процесса

Предыдущая сессия зависла в PHASE 5. Восстанавливаю с checkpoint.

**Инструкции:**
1. Прочитай: UPMT/prompts/utils/RECOVERY_PROTOCOL.md
2. Выполни: СЦЕНАРИЙ A (автоматическое восстановление с JSON checkpoint)
3. Выведи recovery status report
4. Дождись моего подтверждения
5. Продолжи bootstrap с определенной точки

**Критично:**
- НЕ начинай с PHASE 1!
- НЕ пересоздавай файлы из batch 1!
- ИСПОЛЬЗУЙ контекст из предыдущих фаз!
- ПРОДОЛЖИ с batch 2!

Начинай recovery analysis.
```

### Ожидаемый результат

#### Claude должен:

1. **Прочитать checkpoint:**
   ```
   📍 CHECKPOINT DETECTED:
      Phase: PHASE 5: Documentation
      Batch: 1
      Timestamp: [дата/время]
      Files created: [N файлов]
   ```

2. **Показать recovery dialog:**
   ```
   ╔══════════════════════════════════════════════════════╗
   ║        НЕЗАВЕРШЕННЫЙ BOOTSTRAP ОБНАРУЖЕН              ║
   ╚══════════════════════════════════════════════════════╝
   
   ⚠️  Найден checkpoint от: [timestamp]
       Возраст: [X] часов назад
   
   📍 Последнее завершенное:
      Фаза: PHASE 5: Documentation (PHASE 5)
      Батч: 1
      ...
   ```

3. **Предложить варианты:**
   - 1. Продолжить с checkpoint (рекомендуется)
   - 2. Начать заново
   - 3. Показать детальный статус

4. **После выбора "1":**
   - Загрузить контекстные файлы
   - Продолжить с PHASE 5, batch 2
   - НЕ пересоздавать файлы из batch 1

5. **Создать новый checkpoint после batch 2:**
   ```
   💾 Checkpoint saved: PHASE 5: Documentation (batch 2)
      Files: [N], Time: [HH:MM:SS]
   ```

### Критерии успеха

**✅ PASSED если:**
- [ ] Recovery dialog показан с корректной информацией
- [ ] Checkpoint age < 24 часов
- [ ] Phase = 5, Batch = 1
- [ ] Предложено продолжить с batch 2
- [ ] После подтверждения начал с batch 2
- [ ] НЕ пересоздал файлы из batch 1
- [ ] Новый checkpoint создан после batch 2
- [ ] Bootstrap завершился успешно

**❌ FAILED если:**
- [ ] Начал с PHASE 1 (игнорировал checkpoint)
- [ ] Пересоздал файлы из batch 1
- [ ] Не показал recovery dialog
- [ ] Не загрузил контекстные файлы
- [ ] Пропустил модули

### Отладка при провале

**Если Claude начал с PHASE 1:**

Причины:
1. Не прочитал `.upmt/checkpoints/latest.json`
2. Функция `check_for_incomplete_bootstrap()` не вызвана
3. Orchestrator.md ШАГ 0.0.0 пропущен

Решение:
- Явно попросить прочитать checkpoint файл
- Вставить recovery prompt снова

**Если пересоздал файлы:**

Причины:
1. Не проверил существующие файлы перед созданием
2. Не использовал `state['files_created']` из checkpoint

Решение:
- Добавить проверку в phase-5-documentation.md

---

## 🧪 TEST 2: Восстановление по Git (без JSON)

### Цель

Проверить СЦЕНАРИЙ B - восстановление только по Git истории когда JSON checkpoint отсутствует.

### Предварительные условия

- Проект: Brain-Rent
- Bootstrap дошел до PHASE 3 complete
- Git checkpoint существует

### Шаги выполнения

#### Шаг 1: Запуск bootstrap до PHASE 3

```markdown
Прочитай и выполни: UPMT/start/1.3.md
```

Дождаться завершения PHASE 3.

#### Шаг 2: Удалить JSON checkpoint

```bash
# В терминале
cd "C:\Users\333\Documents\My projects\Brain-Rent"

# Удалить JSON checkpoints
rm .upmt/checkpoints/latest.json

# Проверить Git историю (должна остаться)
git log --oneline -5 --grep="bootstrap"
```

#### Шаг 3: Закрыть Claude Code

Симулировать зависание - закрыть вкладку.

#### Шаг 4: Запустить recovery (СЦЕНАРИЙ B)

Открыть новую сессию Claude Code, вставить:

```markdown
🛡️ RECOVERY MODE - Git History Recovery

Bootstrap прервался, JSON checkpoint отсутствует. Восстанавливаю по Git истории.

**Инструкции:**
1. Прочитай: UPMT/prompts/utils/RECOVERY_PROTOCOL.md
2. Выполни: СЦЕНАРИЙ B (восстановление по Git)
3. Определи последний checkpoint из Git истории
4. Проанализируй созданные файлы
5. Создай recovery checkpoint
6. Продолжи с определенной фазы

**Шаги:**
1. Выполни: `git log --oneline --grep="bootstrap" -20`
2. Определи последний "phase complete" коммит
3. Проверь файлы: `extracted_features.md`, `modules_list.md`, `metadata.yaml`, `PROJECT_SYNTHESIS.md`
4. Определи: с PHASE 4 продолжить
5. Создай recovery checkpoint
6. Продолжи bootstrap

Начинай recovery analysis.
```

### Ожидаемый результат

#### Claude должен:

1. **Выполнить git log:**
   ```bash
   git log --oneline --grep="bootstrap" -20
   ```

2. **Определить последний checkpoint:**
   ```
   📍 LAST GIT CHECKPOINT:
      Commit: abc123d
      Message: docs(bootstrap): PHASE 3 complete
      Phase: 3
   ```

3. **Проанализировать файлы:**
   ```
   ✅ extracted_features.md exists
   ✅ modules_list.md exists  
   ✅ metadata.yaml exists (filled)
   ✅ final-tech-stack.md exists
   ❌ PROJECT_SYNTHESIS.md missing → Resume from PHASE 4
   ```

4. **Создать recovery checkpoint:**
   ```json
   {
     "phase": 4,
     "phase_name": "PHASE 4: Synthesis",
     "recovery_mode": true,
     "recovered_from": "git_history",
     "timestamp": "...",
     "state": {
       "detected_files": [...],
       "resume_point": "PHASE 4"
     }
   }
   ```

5. **Продолжить с PHASE 4:**
   - Прочитать контекстные файлы
   - Начать PHASE 4: Synthesis
   - Создать PROJECT_SYNTHESIS.md
   - Продолжить до конца

### Критерии успеха

**✅ PASSED если:**
- [ ] Git история прочитана
- [ ] Последний checkpoint определен (PHASE 3)
- [ ] Файлы проанализированы
- [ ] Recovery checkpoint создан
- [ ] Продолжил с PHASE 4
- [ ] Bootstrap завершился

**❌ FAILED если:**
- [ ] Начал с PHASE 1
- [ ] Не проанализировал Git историю
- [ ] Не создал recovery checkpoint
- [ ] Пропустил PHASE 4

---

## 🧪 TEST 3: Откат к стабильной точке

### Цель

Проверить СЦЕНАРИЙ C - откат к стабильному checkpoint при критической ошибке.

### Предварительные условия

- Bootstrap дошел до PHASE 5
- Файлы созданы, но некоторые повреждены

### Шаги выполнения

#### Шаг 1: Запуск bootstrap до PHASE 5

```markdown
Прочитай и выполни: UPMT/start/1.3.md
```

Дождаться начала PHASE 5.

#### Шаг 2: Симулировать поврежденные файлы

```bash
# В терминале
cd "C:\Users\333\Documents\My projects\Brain-Rent"

# Удалить несколько созданных файлов
rm docs/requirements/dashboard.md
rm docs/requirements/user-profile.md

# Создать пустые файлы (повреждение)
touch docs/requirements/corrupted.md
```

#### Шаг 3: Запустить recovery (СЦЕНАРИЙ C)

Открыть новую сессию Claude Code, вставить:

```markdown
🛡️ CRITICAL RECOVERY - Откат к стабильной точке

Обнаружены поврежденные файлы в PHASE 5. Откатываюсь к последнему стабильному checkpoint.

**Инструкции:**
1. Прочитай: UPMT/prompts/utils/RECOVERY_PROTOCOL.md
2. Выполни: СЦЕНАРИЙ C (откат к стабильной точке)
3. Найди последний "phase complete" коммит (НЕ batch)
4. Откатись к нему
5. Создай backup поврежденных файлов
6. Перезапусти с чистой точки

**Шаги:**
1. `git log --oneline --grep="bootstrap" -20`
2. Найти: `docs(bootstrap): PHASE 4 complete`
3. Создать backup: `git stash push -m "recovery-backup-[date]"`
4. Откат: `git reset --hard [commit_hash]`
5. Верифицировать файлы
6. Перезапустить PHASE 5

Начинай critical recovery.
```

### Ожидаемый результат

#### Claude должен:

1. **Определить стабильный checkpoint:**
   ```
   📍 STABLE CHECKPOINT:
      Commit: def456g
      Message: docs(bootstrap): PHASE 4 complete
      Phase: 4
   ```

2. **Создать backup:**
   ```bash
   git stash push -m "recovery-backup-20251115-143000"
   ```

3. **Откатиться:**
   ```bash
   git reset --hard def456g
   ```

4. **Верифицировать:**
   ```
   ✅ extracted_features.md valid
   ✅ modules_list.md valid
   ✅ metadata.yaml filled
   ✅ PROJECT_SYNTHESIS.md exists
   ✅ Ready to restart PHASE 5
   ```

5. **Перезапустить PHASE 5:**
   - Начать с чистого состояния
   - Создать все requirements файлы заново
   - Завершить bootstrap

### Критерии успеха

**✅ PASSED если:**
- [ ] Стабильный checkpoint определен
- [ ] Backup создан
- [ ] Git откат выполнен
- [ ] Файлы верифицированы
- [ ] PHASE 5 перезапущена
- [ ] Bootstrap завершился

**❌ FAILED если:**
- [ ] Не создал backup
- [ ] Откатился к batch checkpoint (нестабильно)
- [ ] Не верифицировал файлы
- [ ] Не перезапустил фазу

---

## 🧪 TEST 4: Полный цикл с checkpoints

### Цель

Проверить что checkpoints создаются автоматически после КАЖДОЙ фазы и батча.

### Предварительные условия

- Чистый проект
- UPMT с интегрированным Recovery Protocol

### Шаги выполнения

#### Шаг 1: Запуск полного bootstrap

```markdown
Прочитай и выполни: UPMT/start/1.3.md
```

#### Шаг 2: Мониторинг checkpoints

**После каждой фазы проверять:**

```bash
# В терминале (параллельно с Claude Code)
watch -n 10 'ls -lah .upmt/checkpoints/ 2>/dev/null || echo "No checkpoints yet"'
```

**ИЛИ периодически:**

```bash
ls -lah .upmt/checkpoints/
cat .upmt/checkpoints/latest.json | jq '.phase, .phase_name'
```

### Ожидаемый результат

**После завершения bootstrap должны существовать:**

```
.upmt/checkpoints/
├── latest.json                      # phase: 8
├── phase-1.json
├── phase-2.json
├── phase-3.json
├── phase-4.json
├── phase-5-batch-1.json
├── phase-5-batch-2.json
├── phase-5-batch-3.json             # (если 3 батча)
├── phase-5.json                     # финальный
├── phase-6.json
├── phase-7.json
└── phase-8.json
```

**Проверка каждого checkpoint:**

```bash
# Для каждого файла
for file in .upmt/checkpoints/*.json; do
    echo "=== $file ==="
    cat "$file" | jq '.phase, .phase_name, .timestamp'
done
```

### Критерии успеха

**✅ PASSED если:**
- [ ] Все checkpoint файлы созданы
- [ ] `latest.json` указывает на PHASE 8
- [ ] Каждый checkpoint валиден (JSON parse успешен)
- [ ] Timestamps последовательны
- [ ] Files_created списки корректны
- [ ] Git commits соответствуют checkpoints

**❌ FAILED если:**
- [ ] Какой-то checkpoint отсутствует
- [ ] JSON невалиден
- [ ] Timestamps не последовательны
- [ ] Files_created списки пусты

---

## 📊 TEST REPORT TEMPLATE

После выполнения тестов заполните:

```markdown
# Recovery Protocol Test Results

**Дата тестирования:** [YYYY-MM-DD]
**Тестировщик:** [Имя]
**Проект:** Brain-Rent
**UPMT версия:** 3.0.2

---

## Test 1: Симуляция зависания в PHASE 5

**Статус:** [✅ PASSED / ❌ FAILED]

**Результаты:**
- Recovery dialog показан: [✅ / ❌]
- Checkpoint информация корректна: [✅ / ❌]
- Продолжил с batch 2: [✅ / ❌]
- Файлы не пересозданы: [✅ / ❌]
- Новый checkpoint создан: [✅ / ❌]

**Проблемы:** [описание, если есть]

**Скриншоты/Логи:** [ссылки, если есть]

---

## Test 2: Восстановление по Git

**Статус:** [✅ PASSED / ❌ FAILED]

**Результаты:**
- Git история прочитана: [✅ / ❌]
- Checkpoint определен: [✅ / ❌]
- Recovery checkpoint создан: [✅ / ❌]
- Продолжил с правильной фазы: [✅ / ❌]

**Проблемы:** [описание]

---

## Test 3: Откат к стабильной точке

**Статус:** [✅ PASSED / ❌ FAILED]

**Результаты:**
- Стабильный checkpoint найден: [✅ / ❌]
- Backup создан: [✅ / ❌]
- Откат выполнен: [✅ / ❌]
- Фаза перезапущена: [✅ / ❌]

**Проблемы:** [описание]

---

## Test 4: Полный цикл checkpoints

**Статус:** [✅ PASSED / ❌ FAILED]

**Результаты:**
- Все checkpoints созданы: [✅ / ❌]
- JSON валиден: [✅ / ❌]
- Timestamps последовательны: [✅ / ❌]

**Список checkpoints:**
```
[вставить ls -lah .upmt/checkpoints/]
```

**Проблемы:** [описание]

---

## Итоги

**Общий статус:** [✅ ALL PASSED / ⚠️ PARTIAL / ❌ FAILED]

**Рекомендации:**
1. [рекомендация 1]
2. [рекомендация 2]

**Готовность к production:** [✅ / ❌]
```

---

## 🔧 TROUBLESHOOTING

### Проблема: Checkpoint не создается

**Симптомы:**
- `.upmt/checkpoints/` пуста
- `latest.json` не появляется

**Причины:**
1. Функция `save_checkpoint()` не вызывается
2. Путь к файлу некорректен
3. Нет прав на запись

**Решение:**
1. Проверить phase-*.md файлы - секция CHECKPOINT обновлена?
2. Проверить права: `ls -la .upmt/`
3. Создать директорию вручную: `mkdir -p .upmt/checkpoints`

### Проблема: Recovery dialog не показывается

**Симптомы:**
- Bootstrap начинается с PHASE 1
- Checkpoint игнорируется

**Причины:**
1. Orchestrator.md ШАГ 0.0.0 не выполняется
2. Функция `check_for_incomplete_bootstrap()` не вызывается
3. Checkpoint age > 24 часов

**Решение:**
1. Проверить orchestrator.md - ШАГ 0.0.0 добавлен?
2. Явно попросить Claude проверить checkpoint:
   ```markdown
   Перед началом проверь: существует ли .upmt/checkpoints/latest.json?
   Если да - прочитай и покажи recovery dialog!
   ```

### Проблема: Файлы пересоздаются

**Симптомы:**
- Files из batch 1 пересозданы при resume с batch 2

**Причины:**
1. Claude не проверяет существующие файлы
2. Не использует `state['files_created']`

**Решение:**
1. Добавить проверку в phase-5-documentation.md:
   ```python
   if file_exists("docs/requirements/module-1.md"):
       skip_file("module-1.md")
   ```

---

**Версия:** 1.0  
**Статус:** Ready for testing  
**Использование:** См. `RECOVERY_PROTOCOL.md` для контекста

---

**Готово к тестированию на Brain-Rent проекте** ✅

