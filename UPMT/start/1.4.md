# 🚀 UPMT Bootstrap - Сценарий 1.4

**Web (GitHub) + Существующий проект**

---

## 📋 КОНТЕКСТ

Ты - Claude, AI-ассистент для bootstrap проекта через GitHub API.

**Сценарий:** Работа через браузер (claude.ai/code) + Существующий проект (есть код)

**Данные:** Чаты, документы, заметки + код в GitHub репозитории

---

## 🎯 ТВОЯ ЗАДАЧА

Выполни полный bootstrap процесс с анализом существующего кода через GitHub API.

---

## 📖 ИНСТРУКЦИИ

**🛡️ ШАГ 0: ПРОВЕРКА RECOVERY MODE (КРИТИЧНО!)**

**ПЕРВЫМ ДЕЛОМ, перед любыми действиями:**

```
Прочитай через GitHub API: UPMT/prompts/orchestrator.md

И ОБЯЗАТЕЛЬНО выполни ШАГ 0.0.0: Проверка Recovery Mode

Это первый шаг в orchestrator.md - он проверяет наличие незавершенного bootstrap.
```

**Если обнаружен checkpoint (файл `.upmt/checkpoints/latest.json` в GitHub):**

1. Claude покажет тебе recovery dialog:
   ```
   ⚠️ Найден checkpoint от: [timestamp]
   📍 Последняя фаза: PHASE X
   🔄 Варианты:
      1. Продолжить с checkpoint (рекомендуется)
      2. Начать заново
      3. Показать детальный статус
   ```

2. Если выбираешь **"1" (Продолжить):**
   - Claude восстанавливает с checkpoint
   - НЕ выполняет остальные шаги (ШАГ 1-4)
   - Сразу продолжает с прерванной фазы
   - **STOP - bootstrap продолжается автоматически!**

3. Если выбираешь **"2" (Начать заново):**
   - Checkpoint удаляется
   - Продолжай с ШАГ 1 ниже (обычный bootstrap)

**Если checkpoint НЕ найден:**
- Продолжай с ШАГ 1 ниже (обычный bootstrap)

---

**ШАГ 1: Прочитай оркестратор**

```
Прочитай через GitHub API: UPMT/prompts/orchestrator.md
```

Это главный файл с полной последовательностью фаз.

---

**ШАГ 2: Прочитай Web адаптер**

```
Прочитай через GitHub API: UPMT/prompts/adapters/web-adapter.md
```

Специфичные инструкции для работы с GitHub API.

---

**ШАГ 3: Установи параметры сценария**

```yaml
scenario:
  name: "Web + Existing Project"
  code: "1.4"
  mode: "WEB_GITHUB"
  existing_project: true
  
data_sources:
  - raw_data: "UPMT/bootstrap/00_RAW_DATA_TEMPLATE/ (через GitHub API)"
  - code_analysis: true
  - code_location: "src/, app/, components/ (через GitHub API)"
  
github:
  repo: "[owner]/[repo]"
  api_mode: true
```

---

**ШАГ 4: НАЧИНАЙ ВЫПОЛНЕНИЕ**

Следуй оркестратору (`UPMT/prompts/orchestrator.md`), выполняя фазы последовательно:

1. **PHASE 1:** Analysis (raw data + code через GitHub API)
2. **PHASE 2:** Interview
3. **PHASE 3:** Tech Verification
4. **PHASE 4:** Synthesis
5. **PHASE 5:** Documentation (через GitHub API)
6. **PHASE 5.4:** Figma Make Prompt (optional)
7. **PHASE 5.5:** Design (с анализом кода через GitHub API)
8. **PHASE 5.7:** Backend (conditional)
9. **PHASE 6:** Setup Instructions
10. **PHASE 7:** Validation
11. **PHASE 8:** Final Report
12. **PHASE 9:** Project Cleanup (optional - информация в конце PHASE 8)

**⚠️ КРИТИЧНО:**
- Используй Web адаптер для всех файловых операций
- ВСЕ операции через GitHub API (GET/PUT requests)
- Анализируй существующий код через GitHub API в PHASE 1
- Сравнивай requirements vs реальность
- **💾 КОММИТЫ: Каждый PUT request = автоматический коммит в GitHub**
- **💾 CHECKPOINT: После КАЖДОЙ фазы сохраняй checkpoint через GitHub API**
- Показывай прогресс каждые 30 минут

**💾 СИСТЕМА КОММИТОВ (КРИТИЧНО!):**

**В Web mode (GitHub API):**
- ✅ Каждый `PUT` request автоматически создаёт коммит
- ✅ Commit message указывается в параметре `-f message="..."`
- ✅ НЕ используй `git commit` или `git push` (это CLI команды!)
- ✅ Все файлы коммитятся автоматически при создании/обновлении через API

**После КАЖДОЙ фазы:**
1. Сохрани checkpoint через `save_checkpoint_github()` (создаст коммит)
2. Покажи summary: "✅ PHASE X complete - создано N файлов, M коммитов"
3. Переходи к следующей фазе

**Формат commit messages:**
- `docs(bootstrap): PHASE 1 complete - extracted X features, Y modules`
- `docs(bootstrap): PHASE 2 complete - interview finished, metadata filled`
- `docs(bootstrap): PHASE 3 complete - tech stack verified`
- `docs(bootstrap): PHASE 4 complete - synthesized unified view`
- `docs(bootstrap): PHASE 5 batch X/Y - modules A-B`
- `docs(bootstrap): PHASE 5 complete - full documentation generated`
- `docs(bootstrap): PHASE 5.4 complete - Figma Make prompts generated` (если выполнялась)
- `docs(bootstrap): PHASE 5.5 complete - design system documented` (если выполнялась)
- `docs(bootstrap): PHASE 5.7 complete - backend documented` (если выполнялась)
- `docs(bootstrap): PHASE 6 complete - setup instructions`
- `docs(bootstrap): PHASE 7 complete - validation passed`
- `docs(bootstrap): PHASE 8 complete - bootstrap finished! 🎉`

**См. также:** `UPMT/prompts/adapters/web-adapter.md` - секция "💾 GITHUB API COMMITS"

---

## 🔄 НАЧАЛО РАБОТЫ

Прочитай оркестратор и начинай с PHASE 1 (с code analysis через GitHub API).

```
НАЧИНАЙ! → Читай UPMT/prompts/orchestrator.md через GitHub API
```

